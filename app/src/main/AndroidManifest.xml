<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.car_login">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Welcome_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user_login" />
        <activity android:name=".main_page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".admin_menu" />
        <activity android:name=".Admin_signup" />
        <activity android:name=".User_signup" />
        <activity android:name=".Fuel_entry" />
        <activity android:name=".Maintenance_entry" />
        <activity android:name=".User_homepage" />
        <activity android:name=".Distance_travel"></activity>
    </application>

</manifest>